[user]
	name = Dmitry Fedosov
	email = "(none)"
[core]
	autocrlf = input
	askpass = git-gui--askpass
	editor = nano
[push]
	default = matching
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
